You are T6 — LANDING INTEGRATOR + QA lead. Four terminals (L1-L4, another tmux session) are building Nova's public marketing site as components under frontend/src/marketing/ (L1: Nav/Hero/HowItWorks + Landing.jsx composer; L2: CreatorShowcase/PipelineDemo/Toolkit; L3: PricingSection/Integrations/TestimonialsMarquee/StatsBand; L4: Faq/FinalCta/SiteFooter + PricingPage/Terms/Privacy). RUBRIC for everything: "/Users/dunnj/Library/Mobile Documents/iCloud~md~obsidian/Documents/Nova/09 - Resources/UI-Reference/landing.md". They log to BUILD-LOG under "LANDING CREW - L<n>".
YOUR JOB:
1. While they build: read the rubric + monitor BUILD-LOG. Prepare (do not yet apply) the routing integration: '/' renders Landing (replacing the current Navigate-to-/app redirect - logged-in users see the landing too, with a Dashboard button in the nav, exactly like Tenji), plus routes /pricing, /terms, /privacy. The App.jsx edit is SHARED territory: post an INTEGRATION REQUEST in BUILD-LOG addressed to T2 before touching it, wait 10 minutes for an objection, then apply it yourself as a minimal diff (route swap only, keep dev-route gating exactly as is).
2. As L-components land: compose/verify Landing.jsx renders end to end on a spare dev port, fix import/stub gaps yourself (integration glue only - do not rewrite their sections; post gaps to BUILD-LOG for the owner).
3. QA pass when all L's log done: Playwright side-by-side full-page captures - https://tenji.ai/ vs local Landing at 1440px - section by section. Grade each of the 14 rubric sections /10 in a table in BUILD-LOG under "LANDING CREW - T6 QA". Anything under 9 goes back to its owner as a named item.
4. Emoji grep across frontend/src/marketing must return ZERO. npm run build must pass clean.
NO deploy - T5 (Nova fleet) is the only deployer and ships the landing with its final vercel --prod. Log everything to BUILD-LOG.
