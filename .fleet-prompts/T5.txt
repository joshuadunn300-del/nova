ORCHESTRATOR RE-DRIVE. You are T5 (Billing, Client Sites, publish footer, final QA, SOLE deployer). Live verify: your files untouched; also ANOTHER mid-pass deploy shipped (rule 7 broken) - deploy freeze is ON: nobody runs vercel but you, ONCE, at the end. Execute per UI-PARITY-ORDERS.md (vault root, TERMINAL 5):
1. Billing.jsx 1:1 (billing.md + NEW-2 pricing): CURRENT PLAN card - "Free Trial", credits left, monthly limit, "x of y remaining", locked feature grid (Custom Domains / Script Generator / Full Proposals / Per-Client Analytics / Team Members / Priority Support). Plans: Free Trial + $39/$79/$149 cards with site caps 3/15/infinity, lucide icons Zap/Globe/Shield/Users, "Most Popular" pink glow on PRO, "Best Value" on AGENCY, checks incl. Scripts on Pro/Agency, Monthly/Yearly (Save 20%) toggle, current plan CTA shows "Active Plan" pill. History empty state: "No billing history yet. Your invoices will appear here once payments are connected." Bottom: red full-width "Cancel Plan" row. Kill the lightning + lock emojis. Prices read from entitlements.js PLANS once T2 merges T1's extension - single source of truth, coordinate in BUILD-LOG.
2. ClientSites.jsx cards per client-sites.md: browser-chrome mock header (3 dots) + "draft preview" label; hero thumbnail render; status pill overlay top-right Live (green) / Draft (grey); title; meta "niche - date - domain status"; buttons row 1 "Open Designer" (pink, brush) / Preview (eye) / Domain (globe); row 2 Duplicate / Delete. Header: eyebrow DESIGNER, H1 Client Sites, sub "All generated and published websites for your leads and clients.", top-right "Generate New Site" (pink), pill "x of y published".
3. Publish generator: small muted "Powered by Nova" line in the generated-site footer (live-recon placement per designer.md).
4. ENDGAME - only after T1, T2, T3, T4 have each logged DONE in BUILD-LOG: (a) emoji grep across frontend/src must return ZERO rows; (b) npm run build clean; (c) ONE vercel --prod; (d) re-shoot ALL 12 screens at 1440px vs rubrics + Josh's captures; (e) post PASS/FAIL delta table to BUILD-LOG.
Log + self-grade + screenshots to BUILD-LOG.
