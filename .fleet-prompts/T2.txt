ORCHESTRATOR RE-DRIVE. You are T2 (app chrome). Live verify: AppShell.jsx untouched - your whole TERMINAL 2 section in UI-PARITY-ORDERS.md (vault root) is open, and Josh side-by-sided our sidebar vs Tenji's tonight: ours failed on 7 counts. Execute in this order:
1. Sidebar 1:1: (a) lucide icon on EVERY nav item - grep ~/nova/reference/tenji-frontend-components.js for the real icon names before guessing; (b) logo lockup = icon tile + "nova" wordmark in a soft glowing tile with hairline divider below (replaces plain text div); (c) locks = thin pink lucide Lock, right-aligned, row DIMMED - and ALSO dim+lock Scripts while keeping it clickable (Tenji shows upsell theater on Scripts; copy it). Kill the gold lock emoji everywhere incl. the upgrade modal; (d) LIVE EDITOR pill = outlined soft pill with letterspacing, not solid fill; (e) roomier row rhythm - taller rows, consistent icon gutter, lighter weight; (f) top bar bell -> lucide Bell with unread dot.
2. Loading state: full-screen black, pink spinner in the brand-mark slot, "Entering Nova..." - shown while entitlements are null; never flash the app.
3. Floating support bubble bottom-right on every /app screen (pink circle, MessageCircle icon) -> /app/support.
4. When T1 posts ACTIVATION READY in BUILD-LOG: in AppShell after entitlements load, if needsActivation(entitlements) render ActivationWall with onActivated={refreshEntitlements} BEFORE rendering sidebar/topbar (full takeover). Merge T1's PLANS extension into entitlements.js - $39/$79/$149 + site caps 3/15/infinity SUPERSEDE the old $31/$63/$119.
5. Per-category About photos (~8 category sets: trades, home-services, outdoor, automotive, health, beauty, hospitality, fitness) in imageLibrary.js ABOUT_SERVICES fallback.
6. Extend sections/icons.js so resolveIcon covers every logoIcon value all 59 niches emit; verify navbar+footer logo tiles render icons live.
Acceptance: side-by-side screenshot vs Josh's sidebar captures - 5-second indistinguishable. Do NOT deploy - T5 only. Log + self-grade + screenshots to BUILD-LOG.
