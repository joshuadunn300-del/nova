DELEGATION. YOUR LANE: PUBLISHED-SITE OUTPUT QUALITY - the serveSite/publish pipeline (publish/* + the static HTML/CSS generator). Ensure a published client site is pixel-clean and matches the section components, correct meta/title/OG tags, mobile-responsive, footer 'Powered by Nova'. Do NOT touch the app portal. Diff against LIVE Tenji (Josh trial is active - use the logged-in tenji.ai tab) with getComputedStyle; apply LITERAL values; only a clean numeric MATCH counts. lucide icons only, ZERO emoji. Edit ONLY the files in your lane - do NOT touch AppShell, index.css tokens, LeadSearch, Templates, or Billing (orchestrator owns those). Commit + git push origin when each screen is done (repo now on GitHub). Do NOT run vercel - orchestrator handles all deploys. Log to BUILD-LOG.
