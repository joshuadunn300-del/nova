DELEGATION. YOUR LANE: the LONG-TAIL + WALLS - app/pages/Settings.jsx, Credits.jsx, Notifications.jsx, Discovery.jsx, SupportTickets.jsx, and tools ScriptGenerator, Contracts (Pro wall), Analytics (Pro wall), Team (Agency wall). Exact 1:1 vs live Tenji incl. exact wall eyebrow/heading/body/CTA. Diff against LIVE Tenji (Josh trial is active - use the logged-in tenji.ai tab) with getComputedStyle; apply LITERAL values; only a clean numeric MATCH counts. lucide icons only, ZERO emoji. Edit ONLY the files in your lane - do NOT touch AppShell, index.css tokens, LeadSearch, Templates, or Billing (orchestrator owns those). Commit + git push origin when each screen is done (repo now on GitHub). Do NOT run vercel - orchestrator handles all deploys. Log to BUILD-LOG.
