HOTFIX — do this first, takes 5 minutes, Josh caught it live: rubber-band overscroll flashes WHITE above the app. Root cause: the html element still has default background. In index.css set html { background-color: #08080c; } (and body to match), plus overscroll-behavior-y: none on html/body if it does not fight scroll UX. Verify by pulling past the top on localhost at /app, /app/leads, /login — zero white anywhere, matches Tenji's dark overscroll. Applies globally (also covers the marketing landing when it mounts). Then continue your queued BUNDLE DIET order. Log as "T2 - overscroll fix".
