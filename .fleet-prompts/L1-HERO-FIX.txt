CRITICAL BUG (Josh + live side-by-side vs tenji.ai): the hero HEADLINE does not render on production. The giant H1 "The AI That Finds Local Businesses You Can Sell To" is invisible - a black gap sits between the NEW pill and the sub-copy. Cause: your word-reveal animation initializes each word at opacity 0 (and/or translateY) and the entrance animation never fires on the built/deployed bundle, so the words stay invisible. FIX NOW:
1. Guarantee the RESTING state is fully visible: every word must end at opacity:1, transform:none. If using framer-motion/IntersectionObserver, add a fallback so that with no JS animation the text is still visible (animate FROM hidden TO visible, never leave it stuck hidden; set initial only when animation is actually wired; or use CSS animation with the final keyframe = visible and animation-fill-mode: forwards). Test the PRODUCTION build (npm run build + preview), not just dev.
2. Match Tenji exactly: two lines, "Finds" and "Sell" in the pink gradient, rest white, Space Grotesk ~72px bold, tight leading, centered.
3. Add the samurai line-art illustration behind the headline (left third, low opacity, pink tint) - asset at src/assets/samurai.png, same treatment as the login right-panel.
4. Re-verify hero vertical rhythm vs tenji.ai at 1440 AND 375: NEW pill -> headline -> sub -> CTAs, tight spacing.
Playwright screenshot the FIXED hero side-by-side vs https://tenji.ai/ and log as "L1 - HERO FIX" with before/after. This is top priority over any polish. No deploy - flag T5 to redeploy after.
