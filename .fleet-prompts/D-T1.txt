DELEGATION (orchestrator is doing Dashboard/LeadSearch/Templates/Billing/chrome solo). YOUR LANE: the GENERATED demo-site sections in frontend/src/sections/* (Navbar, Hero, Services, About, Testimonials, Faq, Cta, Gallery, Footer). Bring each to 1:1 vs the two recovered real templates (reference/tenji-*-template.html) AND live tenji generated sites, at BOTH desktop (1440) and mobile (375). This is the product Josh pitches with. Diff against LIVE Tenji (Josh trial is active - use the logged-in tenji.ai tab) with getComputedStyle; apply LITERAL values; only a clean numeric MATCH counts. lucide icons only, ZERO emoji. Edit ONLY the files in your lane - do NOT touch AppShell, index.css tokens, LeadSearch, Templates, or Billing (orchestrator owns those). Commit + git push origin when each screen is done (repo now on GitHub). Do NOT run vercel - orchestrator handles all deploys. Log to BUILD-LOG.
