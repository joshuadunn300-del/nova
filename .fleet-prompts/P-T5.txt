HOLD the deploy. Do NOT run vercel yet - wait until the portal-parity sweep (T1-T4) AND L1 hero fix + L2 video fix all log done, then ONE deploy. Meanwhile you own the LONG-TAIL + WALLS: Settings.jsx, ScriptGenerator, Contracts (Pro wall), Analytics (Pro wall), Team (Agency wall), Notifications, Discovery, SupportTickets. getComputedStyle exact-diff vs live Tenji incl. exact wall eyebrow/heading/body/CTA styling. Literal values, clean MATCH. Screenshots + 'T5 - PORTAL PARITY' log. THEN endgame: emoji grep zero, npm run build clean, ONE vercel --prod, post a 20-screen PASS/FAIL delta table.
