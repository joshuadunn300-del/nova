PORTAL PARITY SWEEP (Josh: the client portal is not 1:1 yet). You own Dashboard.jsx, LeadSearch.jsx, Templates.jsx. Run a getComputedStyle exact-diff pass vs live Tenji (use the logged-in Tenji tab in Chrome; if it is behind the activation wall, use reference/tenji-computed-styles.md + the UI-Reference rubrics as the source of truth). Per screen, diff and fix to LITERAL values: stat-card width/radius/shadow/gradient, section spacing/rhythm, font size+weight, icon-tile size/radius/wash, pill styling, exact hex. Only a clean numeric MATCH counts - no eyeballing. Screenshot each side-by-side before/after. Log under 'T1 - PORTAL PARITY' with per-screen /10. No deploy (T5 only).
